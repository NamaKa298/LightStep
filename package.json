{
  "name": "lightstep",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "preinstall": "npm_config_rollup_core=wasm npm install rollup@3"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "react-refresh": "^0.17.0",
    "vite": "^7.1.1"
  }
}
